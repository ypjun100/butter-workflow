# Preferences

## User Feedback
- Prefers that even small low-risk (Track A) tasks produce a written spec and
  pause for approval before implementation, rather than auto-implementing and
  opening a PR straight from start.

## Preference Candidates
- Track A spec should stay lightweight (META + SPEC only) so overhead matches
  change size.

## Rejected Candidates
- None yet.
