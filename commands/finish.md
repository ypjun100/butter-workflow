---
description: Finish a Butter Workflow and capture reusable user preferences.
argument-hint: "[optional task id or context]"
---

Load and follow this plugin's `finish` skill.

User input:

```
$ARGUMENTS
```
