---
description: Review the current Butter Workflow PR or diff using track-specific context.
argument-hint: "[optional PR URL or context]"
---

Load and follow this plugin's `code-review` skill.

User input:

```
$ARGUMENTS
```
